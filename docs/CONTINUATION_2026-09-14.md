# 2026-09-14 续作检查点

## 当前状态

- 分支：`agent/optimize-market-search-and-portfolio`
- 当前模型：`2026-09-14-strategy-platform-v19`
- 已接入：统一时点、横截面因子与策略注册表、三类推荐池、后台全市场任务、原子 JSON 持久化、研究模拟账户、事件生命周期和证据约束解释。
- 本次检查未删除本地 `data/`；该目录已整体加入 `.gitignore`，避免收藏、交易、研究任务和缓存被误提交。

## 已完成验证

- `npm test`：186/186 通过，包含公开接口契约测试。
- `npm run test:ui`：通过，无渲染进程错误。
- `node --check main.js`、`renderer.js`、`preload.js`：通过。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 提示。

## 已修复并待完整回归

1. 研究账户部分报价缺失时保留上次有效估值并标记 `stale`，不再把持仓市值按 0 计算。
2. `trend-breakout` 使用不含当日的此前 20 日高点，并要求最终日线收盘确认；接近前高和盘中越过前高均不能标记“突破确认”。
3. 打包后的大盘 Worker 显式继承主进程数据根目录。
4. `findStockByCode` 已能从研究账户持仓构造详情股票，重启后仍可进入详情。
5. 全市场任务清单新增 `valid/failed` 计数；有缺失时按钮改为只重试失败项，不清空成功结果。
6. 取消任务调整为先终止 Worker，再发布取消状态，避免最后一批结果覆盖取消状态。
7. 原子 JSON 写入前校验现有主文件，损坏主文件不再覆盖上一版有效备份。
8. 研究账户成交后立即按最新行情重新估值；影子实验统计改为“已发布”，不再冒充全部证券已评估。

以上修改及本轮续作已通过完整回归。

## 本轮续作已完成

1. 全市场任务新增逐股历史仓库：首次或定期校准抓取 320 根，普通新交易日抓取 30 根增量合并；同日快照按日期覆盖并保留最近 320 根；收藏、持仓、严格/观察/强势候选优先处理。
2. 用户状态保存改为等待磁盘 IPC 成功回执后再提示标签和模拟交易保存成功；失败时明确说明当前操作仅保留在浏览器缓存。
3. 新增 `STOCK_ASSISTANT_DATA_ROOT` 绝对路径覆盖，测试和诊断任务可显式隔离数据目录；相对路径会被忽略，避免数据意外写入未知工作目录。

## 仍待修复问题

1. 多进程行情请求仍缺少统一来源预算，主进程和 Worker 同时运行时可能各自触发同一来源的限流。
2. 还需补充打包模式数据路径和研究账户独有持仓点击的 Electron 回归。

## 建议续作顺序

1. 实现跨进程统一行情来源预算与退避状态共享。
2. 补安装包数据路径和研究持仓点击的 Electron 回归。
3. 执行真实接口抽查、安装包验证和数据目录检查。

## 复现证据

- 研究账户：有报价时权益约 `109987`，同一账户传入空报价后权益变为 `49987`，持仓市值为 `null`。
- 趋势突破：`close=9.8`、`high20=10`、`volumeRatio=1.8`、均线向上、`rps20=90` 时，当前注册表返回 `trend-breakout / 突破确认`。
- 在线契约：本次大盘分析测试约 50 秒，功能通过但弱网交互时长仍需持续监控。
